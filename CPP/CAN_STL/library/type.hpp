#pragma once
#ifndef _TYPE_H
#define _TYPE_H

namespace Can
{
	using byte_t = char;
	using size_t = unsigned long long;
	using ulong_t = unsigned long;
	using long_t = long;
	using pos_type = int;
	using ptrdiff_t = int;

	using nullptr_t = decltype(nullptr);

}

#endif